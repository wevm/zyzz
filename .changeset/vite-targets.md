---
'zyzz': patch
---

Add `targets()` to `zyzz/vite` for Vite builds that bundle precompiled output without the `zyzz()` plugin. It applies the same browser target defaults and keeps `light-dark()` native.
