/**
 * Identifies compiled style values across independently built module boundaries.
 * @module
 */
/** Shared identity without mutable registration or framework dependencies. */
export const metadata: unique symbol = /* @__PURE__ */ Symbol.for('zyzz.style')
