/**
 * Exposes the optional Node filesystem host for builds and watching.
 * @module
 */
/** File builds and watching, isolated from the pure compiler and styling core. */
export * as Host from './Host.js'
